# v1 prototype — DO NOT USE AS REFERENCE

These files are the original prototype. They contain at least ten defects documented in
`DESIGN_v2.md` §5.1, including an invalid `host_permissions` match pattern that prevents the
extension from loading at all, and a complete absence of any content script.

Kept for history only. Build against `BUILD_P0.md`.
