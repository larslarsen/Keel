node_modules/
dist/
*.zip
graphify-out/
junk/
.DS_Store
.claude/settings.local.json
.claude/*.tmp.*
# Generated by npm run prepare:chrome|firefox — pick one before load-unpacked
extension/manifest.json
# The live daemon: the native-messaging manifest points here, so `go build`
# inside daemon/ deploys it. Ignored because it is a 46 MB artifact, not
# because it should be built elsewhere — it should be built here, and then
# smoke-tested, because building it replaces what the browser is running.
daemon/keel-host
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
src/manifest.json
